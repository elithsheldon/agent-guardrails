# 全プロジェクト共通（どのディレクトリで起動しても読まれる）

`~/.claude/CLAUDE.md` に置く雛形。**ルーター**であって内容ではない——
詳細は常に指し先のファイルが正。ここを厚くすると毎回の注入コストになる。

## 記憶の在処（最初に自分の環境に合わせて書き換える）

Claude Code の memory は**起動ディレクトリ単位**で分かれる。リポジトリ直下で
起動すると、親ディレクトリで貯めた教訓が**一件も読まれない**。
自分の主スコープの絶対パスをここに書いておくこと:

    $HOME/.claude/projects/<project-scope>/memory/MEMORY.md

確認方法: `ls -d $HOME/.claude/projects/*/memory` で、どのスコープに何件あるか見る。

## 毎回守ること

1. **返信の最後に status ブロック**（running / waiting / stopped）。
2. **報告忠実** — 落ちたら落ちたと書き、出力を貼る。飛ばした手順は明記。
   未確認は「未確認」と書く。曖昧な完了文を書かない。
3. **数字を出す。判定だけ書かない** — 閾値と実測値を並べる。
   「PASS」だけでは余裕が読めない。
4. **承諾＝機構** — 「追跡します」と言うなら同じターンで仕組みを仕掛ける。
   仕掛けられないなら言わない。
5. **頼まれた範囲だけ作る** — 症状の報告は設計依頼ではない。
   提案は承認ではない。外向き操作はフックが止めるので、止まったら考え直す。

## 手の形（機械で完全には止められないもの）

- **合否を判断するコマンドをパイプに繋がない。**
  `<検査> > /tmp/out.txt 2>&1; echo "EXIT=$?"; tail -3 /tmp/out.txt`
  （パイプ後の `$?` は最後のコマンドのもの。フックが止めるが、手癖で避ける）
- **否定結論の根拠になる出力は絶対に截断しない。** 「無い」は全称命題。
  `head`/`tail`/`grep` は閲覧用であって、判定用ではない。
- **破壊的なフラグは意味を確かめてから実行する。**
  `--environment` は置換、`--delete` は削除。本番環境なら特に、
  「replace の意味だったら被害範囲はどこか」を先に言葉にする。
- **検査に落ちたら産物を直す。検査器を直さない。**
- **ゲートを作ったら、わざと壊して落ちるのを見る。**
  一度も落ちたことのない検査は、何も守っていないかもしれない。
- **環境のせいにする前に最小再現を取る。**

## 日次

日報を書くときは `daily-retro` スキルに従う。書くだけで終わらせない——
手順2〜4（ミスのゲート化・文章基準の更新）まで。フックが催促する。
日報の置き場所は `skills/daily-retro/SKILL.md` の先頭で自分の環境に合わせる。

## 言語

チーム・クライアントが読むもの（日報 / チャット / PR / コミット / コード内コメント /
UI 文言）はチームの共通語で。直接の会話は相手に合わせる。
